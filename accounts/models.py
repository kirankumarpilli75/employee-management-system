from django.db import models

# This app relies on Django's built-in User model for authentication.
# No custom models are required here. If profile fields for staff users
# are needed in the future (e.g. avatar, role), extend a Profile model here.
